# Git flow Basico
comando para el flujo de git basico
```sh
git status
git add .
git status
git comit -m "Descripcion del cambio en el proyecto"
git push
```
## Nota
Si no dice quien soy reinicio la pc